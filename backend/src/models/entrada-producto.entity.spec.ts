import { EntradaProductoEntity } from './entrada-producto.entity';

describe('EntradaProductoEntity', () => {
  it('should be defined', () => {
    expect(new EntradaProductoEntity()).toBeDefined();
  });
});
